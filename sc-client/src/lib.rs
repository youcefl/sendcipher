use std::fs::File;

pub fn encrypt_file(file_to_encrypt: File) {
    
}

//

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = 2+2;
        assert_eq!(result, 4);
    }
}
